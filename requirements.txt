# ===================== 基础核心依赖 =====================
# Web 框架（FastAPI 是现代异步框架，Flask/Django 可选）
fastapi>=0.115.0,<1.0.0          # 版本锁定：>=最低兼容版，<大版本（避免破坏性更新）
uvicorn[standard]>=0.32.0,<1.0.0 # FastAPI 运行服务器（standard 包含完整功能）

# 数据处理
pydantic>=2.8.0,<3.0.0           # 数据校验（FastAPI 标配）
python-dotenv>=1.0.1,<2.0.0      # 环境变量管理
pandas>=2.2.2,<3.0.0             # 数据分析（按需添加）
numpy>=1.26.4,<2.0.0             # 数值计算（按需添加）

# 数据库相关
sqlalchemy>=2.0.31,<3.0.0        # ORM 框架
psycopg2-binary>=2.9.9,<3.0.0    # PostgreSQL 驱动（MySQL 用 mysql-connector-python）
redis>=5.0.1,<6.0.0              # Redis 客户端（按需添加）

# ===================== 工具类依赖 =====================
requests>=2.32.3,<3.0.0          # HTTP 请求
httpx>=0.27.0,<1.0.0             # 异步 HTTP 请求（FastAPI 推荐）
python-multipart>=0.0.9,<1.0.0   # 文件上传（FastAPI 用）
email-validator>=2.2.0,<3.0.0    # 邮箱校验
passlib[bcrypt]>=1.7.4,<2.0.0    # 密码加密
jwt>=1.3.1,<2.0.0                # JWT 令牌（身份验证）

# ===================== 开发/测试依赖（可选） =====================
# 注：开发依赖建议用 pip install -r requirements-dev.txt 单独管理
# pytest>=8.2.2,<9.0.0            # 单元测试
# black>=24.4.2,<25.0.0           # 代码格式化
# flake8>=7.0.0,<8.0.0            # 代码检查
# coverage>=7.5.0,<8.0.0          # 测试覆盖率